import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { BitPage } from './bit';

@NgModule({
  declarations: [
    BitPage,
  ],
  imports: [
    IonicPageModule.forChild(BitPage),
  ],
})
export class BitPageModule {}
